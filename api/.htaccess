# Only the three endpoints can be opened from a browser. Everything else here is private.
Options -Indexes
<FilesMatch "\.(php|json|txt|md)$">
  Require all denied
</FilesMatch>
<FilesMatch "^(submit|pay|stripe-webhook|status)\.php$">
  Require all granted
</FilesMatch>
